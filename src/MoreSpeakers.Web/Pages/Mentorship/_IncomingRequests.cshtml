@model List<MoreSpeakers.Domain.Models.Mentorship>

@if (!Model.Any())
{
    @await Html.PartialAsync("_IncomingRequests_NoRequestsFound", null)
}
else
{
    @foreach (var request in Model)
    {
        @await Html.PartialAsync("_IncomingRequestCard", request)
    }
}
