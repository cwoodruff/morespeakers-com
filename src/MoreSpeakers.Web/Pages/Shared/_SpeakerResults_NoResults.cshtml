@using MoreSpeakers.Domain.Extensions
@model Web.Models.ViewModels.SpeakerResultsNoSpeakersFoundViewModel

<div class="text-center py-5">
    <i class="bi bi-search display-1 text-muted"></i>
    <h4 class="mt-3">No @Model.SearchType.GetDescription() found</h4>
    <p class="text-muted">Try adjusting your filters to find more @Model.SearchType.GetDescription().</p>
</div>

